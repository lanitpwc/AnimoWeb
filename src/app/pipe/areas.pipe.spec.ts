/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AreasPipe } from './areas.pipe';

describe('AreasPipe', () => {
  it('create an instance', () => {
    let pipe = new AreasPipe();
    expect(pipe).toBeTruthy();
  });
});
